<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
    <h2>User info</h2>
    <%= form_with model:@user, local:true do |f| %>
      <%= render 'layouts/errors', obj: @user %>
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, class: "name" %>
        <!--ターミナルのログを確認すると[Unpermitted parameter: :title]というエラーが出力されています。
これを日本語にすると、[titleというパラメーターは許可されていない]となります。
db/scheme.rbを確認してもusersテーブルにはtitleというカラムは存在しておらず、名前のカラムにはnameが使われています。
今回は名前のアップデートをすることができないため、フォームで送る部分のinputタグに原因があると推測でき、Viewを確認すると名前を入力する箇所の属性がtitleとなっているため、名前のカラムであるnameに修正を行います。-->
      </div>
     <div class="form-group">
        <%= f.label :image %>
        <%= f.file_field :profile_image, class: "form-control-file user_image", accept: 'image/*' %>
      </div>
      <div class="form-group">
        <%= f.label :introduction %>
        <%= f.text_area :introduction, class: "form-control introduction" %>
      </div>
      <div class="form-group">
        <%= f.submit class: 'btn btn-secondary' %>
      </div>
    <% end %>
  </div>
</div>
